using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChatToDate : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(CreateDate.ToDate);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
